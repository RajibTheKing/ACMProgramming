#include<bits/stdc++.h>

using namespace std;

int main()
{
    int N;
    bool arr[105];
    while(scanf("%d", &N)==1){
        if(!N) break;

        memset(arr, 0, sizeof (arr));



    }
    return 0;
}
